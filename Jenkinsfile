pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                #sh 'ansible-playbook /home/salim/ansible-playbooks/image-ansible-playbook.yaml --user salim'
		sh 'whoami'
            }
        }
    }
}
